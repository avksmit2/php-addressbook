<!DOCTYPE html>
<html>
    <head>
        <title>Contacts</title>
    </head>
    <body>
        <div class="container">
            {% if matches is not empty %}
                <h1 class="well text-center">Here are the contacts matching your search:</h1>
                <ul>
                    {% for contact in matches %}
                        <li>{{ contact.getFName }} {{contact.getLName }} <br>
                            Phone: {{ contact.getPhone }} <br>
                            Email address: {{ contact.getEmail }}<br>
                            Address: {{ contact.getStreet }}, {{ contact.getCity }}  {{ contact.getState }}, {{ contact.getZip }} <br>
                            {{ contact.getImg }}
                        </li>
                    {% endfor %}
                </ul>
            {% else %}
                <h1 class="well text-center">There are no contacts matching your search criterion.</h1>
            {% endif %}
            <h3><a href="/">View your list of contacts.</a></h3>
        </div>
    </body>
</html>
